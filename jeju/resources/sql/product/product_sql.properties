selectProductList=SELECT * FROM PRODUCT
selectSearchProductList=SELECT * FROM(SELECT ROWNUM AS RNUM ,A.* FROM(SELECT * FROM PRODUCT WHERE P_NAME LIKE ? OR P_BIG_NAME_ENG LIKE ? OR P_BIG_NAME_KOR LIKE ? OR P_MAP_ADDRESS LIKE ? ORDER BY P_NUM DESC) A) WHERE RNUM BETWEEN ? AND ?
selectSearchProductCount=SELECT COUNT(*) FROM PRODUCT WHERE P_NAME LIKE ? OR P_BIG_NAME_ENG LIKE ? OR P_BIG_NAME_KOR LIKE ? OR P_MAP_ADDRESS LIKE ? 