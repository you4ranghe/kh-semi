selectProductList=SELECT * FROM PRODUCT
selectSearchProductList=SELECT * FROM(SELECT ROWNUM AS RNUM ,A.* FROM(SELECT * FROM PRODUCT_TEST WHERE P_NAME LIKE ? OR P_BIG_NAME_ENG LIKE ? OR P_BIG_NAME_KOR LIKE ? OR P_MAP_ADDRESS LIKE ? ORDER BY P_NUM DESC) A) WHERE RNUM BETWEEN ? AND ?
selectSearchProductCount=SELECT COUNT(*) FROM PRODUCT_TEST WHERE P_NAME LIKE ? OR P_BIG_NAME_ENG LIKE ? OR P_BIG_NAME_KOR LIKE ? OR P_MAP_ADDRESS LIKE ?
clickHeart=INSERT INTO WISH VALUES(?,?)
checkWish=SELECT * FROM WISH WHERE USER_ID=?
selectProductList2= SELECT * FROM(SELECT ROWNUM AS RNUM ,A.* FROM(SELECT * FROM PRODUCT_TEST ORDER BY P_NUM DESC) A) WHERE RNUM BETWEEN ? AND ?
selectProductCount=SELECT COUNT(*) FROM PRODUCT_TEST
addProduct=INSERT INTO PRODUCT VALUES(P_NUM_SEQ.NEXTVAL,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,0,?)